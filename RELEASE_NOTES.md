# 云文档套件 v2.0.1

本版本修复脑图选中节点无法直接输入、Tab 新增节点输入框偏移，以及节点布局变化后连线未及时对齐的问题；同时更新编辑器缓存版本，避免思源继续加载旧资源。

## v2.0.1

- 选中脑图节点后可直接输入文字。
- Tab/Enter 新增节点时自动校正输入框位置和连线。
- 节点样式、层级样式、搜索定位和分支聚焦变化后重新对齐连线。
- 更新插件版本号，确保编辑器资源缓存失效。

## v2.0.0

本版本继续保持思源优先和轻量化边界，集中解决 `.mm` 脑图搜索后的视图污染、键盘新增操作不一致，以及大图重复刷新造成的操作负担。

## v1.9.2

### 搜索与视图状态

- 搜索已折叠节点时仍会临时展开目标祖先，但关闭搜索后会恢复搜索前的收起状态。
- 用户在搜索期间主动展开或收起的分支会被保留，不会被退出搜索时的恢复动作覆盖。
- 搜索期间的临时展开状态不会写入自动保存和导出结果。

### 键盘操作与聚焦

- Tab 新增子节点、Enter 新增后方同级节点、Shift+Enter 新增前方同级节点、Ctrl+Enter 插入父节点后均立即选中新节点并进入输入。
- 所有键盘新增方式共用同一套临时新增跟踪；尚未输入内容时按 Ctrl+Z 可直接撤销整个新增节点。
- Esc 可退出分支聚焦，并恢复进入聚焦前的缩放和画布位置；搜索面板打开时会优先关闭搜索，避免一次按键同时改变两层视图。

### 大图流畅度

- 连续布局刷新合并到统一的双帧任务中，避免同一轮操作重复刷新整张脑图。
- 文字输入期间只调整编辑框，跳过不必要的全图节点装饰与大纲重建。
- 画布选择变化只同步大纲高亮行，不再每次重建完整大纲。
- 新增超过 1000 节点的索引、搜索和大纲压力测试。

### 当前验证

- 43 项自动测试、类型检查、生产构建和发布包结构检查通过。
- Windows 思源真实页面已验证搜索临时展开与恢复、Tab/Enter/Shift+Enter/Ctrl+Enter 新增后直接输入与 Ctrl+Z 撤销、Esc 退出分支聚焦与视口恢复、自动保存和刷新重开。
- 回归后原 26 个节点完整保留、没有测试占位节点，干净页面控制台无错误。
- 当前尚未提交、打标签、推送或上传集市。

---

# 云文档套件 v1.9.1

本版本继续保持轻量化边界，集中提升 `.mm` 脑图的查找、层级浏览、分支聚焦和键盘操作可靠性。

## v1.9.1

### 查找与大纲

- 新增节点大纲侧栏，显示完整层级、折叠状态和当前画布选中节点。
- 支持 Ctrl+F 打开节点查找，Enter/Shift+Enter 或 F3/Shift+F3 在结果间循环跳转。
- 搜索包含已折叠分支；跳转时只展开目标祖先，并将目标节点移动到未被侧栏遮挡的可视区域。
- 点击大纲节点会选中画布节点；画布选择变化也会同步高亮并滚动大纲。

### 键盘与嵌入体验

- Tab 新增子节点后立即选中新节点并进入文字编辑，可直接输入节点内容。
- 新节点尚未输入时按 Ctrl+Z，会先退出临时输入框并撤销整个新增节点。
- Ctrl+Z/Ctrl+Y 在未编辑文字时统一交给脑图历史，键盘新增节点可以可靠撤销和重做。
- 保留节点文字编辑框自己的撤销行为，避免 Ctrl+Z 意外撤销整张脑图。
- 新增可随时打开的快捷键面板，点击“快捷键”或按 `?` 即可查看新增、编辑、移动、查找、撤销等常用操作。
- 新增 Ctrl+/ 收起或展开当前分支，以及 Ctrl+Home 退出分支聚焦并回到中心主题；文字输入期间不会误触这些画布快捷键。
- 节点输入框会随中文输入和长文本自动扩展，在窄窗口中自动换行，降低输入法组合文字期间的遮挡和跳动。
- 扩大思源 iframe 拖动标识的隐藏范围，去除编辑器左侧突兀的地球图标。
- 左侧布局工具继续提供中文悬停说明，查找、大纲、样式和导出入口保持统一。

### 分支聚焦

- 所选节点底部工具栏新增“聚焦分支”，用于暂时隐藏无关分支，减少大型脑图的视觉干扰。
- 聚焦期间顶部始终显示“返回完整脑图”，即使撤销后当前选择被清空，也不需要重新选中节点才能退出。
- 聚焦只改变当前视图；自动保存、导出和恢复仍读取完整脑图，并保留进入聚焦前的左右布局方向。
- 修复收起分支后将其作为聚焦根节点，再用 Tab 添加子节点时访问不存在折叠控件的问题。
- 聚焦状态下执行撤销或重做会安全回到完整历史快照后恢复聚焦，避免历史记录只包含当前子树。

### 验证

- 新增 6 项脑图索引、隐藏节点搜索、祖先展开、大纲过滤、循环导航和快捷键上下文测试；自动测试总数增加到 41 项。
- 类型检查、生产构建和发布包结构检查通过。
- Windows 思源真实页面已验证快捷键帮助、中文长文本输入、Ctrl+/ 分支折叠、分支聚焦、常驻返回入口、聚焦根节点 Tab 新增、Ctrl+Z 撤销、自动保存和刷新重开；原 26 个节点完整保留且控制台无错误。
- 左侧布局工具均具备中文可访问名称；插件嵌入块的地球拖动标识由扩展后的外层选择器隐藏。
- 当前尚未提交、打标签、推送或上传集市。

---

# 云文档套件 v1.9.0

本版本作为轻量表格稳定里程碑，不增加复杂格式兼容范围，集中固化保存、恢复、导出与界面状态行为。

## v1.9.0

### 保存与恢复安全

- `putFile` 返回成功后再次读取思源附件并核对 SHA-256；只有远端字节与目标内容一致时才更新保存基线并清除对应恢复缓存。
- 写入后校验失败会保留本机恢复数据和原保存基线，防止“接口成功但内容未落盘”被误报为已保存。
- 保存失败或用户拒绝覆盖冲突时，不再静默退出编辑模式并覆盖错误提示；编辑界面保持打开以便重试或导出。
- 连续编辑期间仍保留最新恢复快照，较早的在途保存不会清除较新的本机修改。

### 导出与界面收口

- 导出前重新读取生成的 `.xlsx`，核对 Sheet 列表、公式、合并区域以及冻结/筛选/插件图表状态。
- 导出校验失败时停止下载并明确提示；导出期间锁定按钮，避免连续点击生成重复文件。
- 窄窗口下查找面板自动贴合左右边界，选区统计保持在底部可见并省略过长内容。
- 公式候选根据可视窗口重新定位，避免窄编辑区越界。

### 验证

- 自动测试扩充至 35 项，新增写入后远端字节校验、导出成功回读和损坏导出阻止覆盖。
- 类型检查、生产构建和发布包结构检查通过。
- Windows 思源桌面端已验证真实附件修改、写入后校验、刷新重开回读及导出回读；测试文件包含 2 个 Sheet、4 个公式和合并区域，控制台无错误。
- 当前尚未提交、打标签、推送或上传集市。

---

# 云文档套件 v1.8.5

本版本不扩大复杂 Excel 兼容范围，集中统一思源内的单元格输入、取消、选区统计、公式候选和合并单元格导航体验。

## v1.8.5

### 编辑手感

- 选中单元格后直接输入会替换原内容；F2 或双击进入单元格内部编辑，方向键可继续移动光标。
- Enter、Shift+Enter、Tab、Shift+Tab 继续用于提交并移动；Esc 会恢复本次编辑前的单元格、撤销恢复操作和自动保存状态。
- 同一次单元格输入只保留一条最新恢复操作，避免逐字符累积恢复记录，同时仍可在异常关闭时恢复当前内容。
- 合并单元格中的方向键导航会跳过其覆盖范围，进入被合并区域时自动定位到合并起点，避免选区落在不可见单元格上。

### 选区与公式效率

- 底部状态区显示非空计数、数字数量、求和和平均值，文本与日期不会误计入求和。
- 公式栏输入 `=` 或函数前缀时显示常用函数候选，可用上下方向键选择，Enter 或 Tab 填入，光标自动位于括号内。
- 保留函数参数提示与公式错误说明；简约模式仍可直接编辑并自动保存。

### 验证

- 自动测试扩充至 32 项，新增选区统计类型边界和 500×50 全轻量范围压力覆盖。
- Windows 思源桌面端已验证简约模式直接输入与 Esc 取消、F2/双击内部编辑、Enter 提交、合并区域导航、选区统计、Enter/Tab 公式候选、公式计算、自动保存和关闭重开回读，控制台无错误。

---

# 云文档套件 v1.8.4

本版本继续聚焦思源内的轻量表格体验，完善输入、查找替换、公式提示、键盘跳转与批量粘贴反馈，不扩展复杂 Excel 兼容范围，也不引入 Web Worker。

## v1.8.4

### 输入与查找效率

- 新增当前 Sheet 的替换和全部替换，可选择区分大小写、仅当前选区、查找公式原文。
- 批量替换只触发一次公式重算、一次撤销记录和一次恢复操作；默认不会误改公式计算结果。
- 输入严格的 `YYYY-MM-DD` 日期或带 `¥`、`￥` 的人民币金额时，自动保存为真实日期/数值及对应格式；改回普通文本或数字时清除旧的自动格式。
- 公式栏提供常用函数候选与简短参数提示；公式错误提供引用无效、除零、值类型、循环引用和暂不支持说明。
- 新增 Home、End、Ctrl+Home、Ctrl+End 和 Ctrl+方向键区域跳转，并继续支持 Shift 扩展选区。
- 大范围粘贴显示处理中、写入行列数与截断结果，保持一次撤销、一次渲染和自动保存。

### 验证

- 自动测试扩充至 30 项，新增日期/货币往返、格式切换、批量替换恢复和公式错误分类覆盖。
- Windows 思源桌面端已验证大小写/选区/公式查找、替换与一次撤销、日期和人民币输入、公式提示与错误说明、区域跳转、批量粘贴、自动保存和关闭重开回读，控制台无错误。

---

# 云文档套件 v1.8.3

本版本继续完善思源内的轻量表格日常操作，新增选中位置的行列插入与删除，并保持公式、合并、尺寸、冻结、撤销和恢复一致。

## v1.8.3

### 行列结构操作

- 可在当前单元格或选中行上方插入行，并删除当前行或选中的整行区域。
- 可在当前单元格或选中列左侧插入列，并删除当前列或选中的整列区域。
- 行号和列标支持 Shift 与拖动连续选择；右键菜单可直接插入或删除，整行整列选中后也可使用 `Ctrl++`、`Ctrl+-`。
- 多行多列删除前会明确显示数量和范围；操作期间防止重复点击，结束后显示结果并保持原滚动位置和结构选区。
- 插入和删除会同步迁移本 Sheet 与跨 Sheet 公式引用；被删除的单元格引用会明确变为 `#REF!`。
- 合并区域、行高、列宽、冻结范围、筛选列、插件图表范围和工作表可视范围会随结构变化调整。
- 结构操作支持撤销、重做、本机恢复、自动保存和 XLSX 导出回读。
- 超出轻量编辑可视范围的工作表会阻止结构操作，避免误改未显示内容。

### 验证

- 新增行列公式迁移、跨 Sheet 引用、合并/筛选/冻结/图表边界、连续恢复重放和轻量历史快照测试；自动测试共 26 项。
- Windows 思源桌面端已验证插入、删除、撤销、重做、自动保存、重新打开回读，控制台无错误。

---

# 云文档套件 v1.8.2

本版本以 v1.8.1 为稳定基线，继续优化思源内的轻量表格体验，不扩展复杂 Excel 兼容范围，也暂不引入 Web Worker。

## v1.8.2

### 轻量表格体验

- 在编辑模式中可以直接拖动列表头和行表头边缘调整列宽、行高。
- 双击列宽拖动区可按当前内容自动调整宽度；双击行高拖动区恢复默认高度。
- 自定义行列尺寸参与撤销、重做、本机恢复、自动保存和 XLSX 导出回读。
- 输入普通数字时保存为数值单元格，输入百分比时保存为数值与百分比格式，公式计算和排序不再把这些内容当成文本。
- 以零开头的编号默认保留为文本；使用英文单引号前缀可以明确按文本输入。

### 验证

- 自动测试由 16 项增加到 18 项，新增轻量输入类型与行列尺寸往返测试。

---

# 云文档套件 v1.8.1

本版本集中修复脑图层级布局、编辑交互、文档树创建入口和集市图标，并继续完善 Excel 简约模式与常用格式工具。附件仍通过思源内核 API 写回，原有自动保存、同步修订标记、冲突保护和本机恢复机制保持不变。

## v1.8.1

- 脑图同一层级统一使用精确中心线算法，修复深层节点、连接线、收缩按钮和数字圆圈的垂直偏差。
- 连接线改为稳定的直线或小圆角正交线，节点及收缩按钮始终位于连线上层，避免穿透文字、选框和圆圈。
- 收缩按钮改为悬停显示的方向缩略箭头；收起后显示子节点数量，并扩大无形命中区域以保持易操作性。
- 展开和收起前锁定被点击节点的屏幕中心，刷新后恢复原坐标，避免多子节点切换时画布向下漂移。
- 脑图选中后可直接输入，完成状态可重复点击取消；撤销、重新打开和左右布局切换后继续保持对齐。
- Excel 简约模式支持直接编辑；补充区域格式、合并、填充、筛选、排序、冻结和图表等常用操作及恢复记录。
- 文档树右键菜单新增一级“创建文件”，可选择新建脑图、Word 文档或 Excel 工作簿，同时保留“云文档套件”插件状态入口。
- 修复表格编辑器依赖版本被固化为旧版本的问题，确保 Windows、Linux 与 NAS/Docker 加载同一套最新脚本。
- 集市图标保持 160×160 透明背景，压缩为 4,283 字节，低于官方 20KB 限制；发布包内图标与仓库图标保持一致。
- 发布前完成 16 项自动测试、类型检查、生产构建、发布包完整性检查及本机思源真实交互回归。

## v1.8.0

- 新增拖动或 Shift 扩展的连续区域选择，统一显示浅蓝选区和完整外框。
- 新增多单元格复制、TSV 粘贴和区域 Delete 清空。
- 新增最多 100 步单元格撤销/重做。
- 新增 Enter、Tab、方向键导航，以及 Ctrl+F 当前 Sheet 查找。
- 支持单元格引用、区域引用、四则运算、幂与括号，以及 `SUM`、`AVERAGE`、`MIN`、`MAX`、`COUNT`、`ROUND`、`ABS`、`IF`。
- 公式结果会随引用单元格变化刷新；不支持的新公式明确显示“#暂不支持”，原工作簿中的复杂公式结构继续保留。
- 公式、单元格样式和区域快照可随撤销、恢复和自动保存正确保留。
- 重绘集市图标外缘，移除黑色噪点并使用透明圆角和柔和阴影。
- 发布前完成 15 项自动测试、类型检查、本机思源真实复制粘贴、撤销恢复、公式写回重开及图标缩略预览验证。

## v1.7.0

- 默认使用层级样式：根节点为蓝色方块，一级节点为无边框浅灰方块，深层节点为简洁文字。
- 连接线改为水平直线、短竖向主干和小圆角，取消大幅弧线；左右与双向布局均可保存。
- 新节点和旧版占位文字统一显示“输入文字”，未填写节点不再生成多余灰色外框。
- 下划线固定为 1px，并可与已完成删除线同时显示。
- 移除 `.mm` 中不需要的概要、自定义连接和双向连接菜单及存储数据。
- 明确左键拖动节点调整层级、右键拖动画布平移视图。
- 新建脑图和 Excel 每次使用唯一附件，避免删除文档后旧附件或旧恢复缓存被复用。
- 增加远端附件删除保护、跨设备冲突保护、连续保存恢复保护和 Excel Sheet 删除恢复测试。
- 发布前完成 12 项自动测试、类型检查、生产依赖漏洞审计和本机真实删除/自动保存/重新打开验证。

## v1.6.8

- 工具栏“任务”改为“完成”，点击后直接切换已完成/未完成状态。
- 已完成节点使用灰色文字和删除线，取消完成后恢复原有颜色与下划线格式。
- 移除节点文字前的空心任务圆点，视觉行为与参考效果一致。
- 新增可切换并写入 `.mm` 文件的“层级样式”：根节点为蓝色块、一级节点为浅灰圆角块、深层节点使用简洁文字和蓝色连接线。
- 彩色和层级样式下，节点输入框均显示白底蓝色圆角边框，结束输入后恢复原节点样式。

## v1.6.7

- 云文档 iframe 插入页面时立即完成尺寸计算，避免先显示 720px 旧尺寸再突然放大。
- 文档树监听仍保留 120ms 防抖，尺寸适配不再等待防抖任务。

## v1.6.6

- 脑图和表格功能区动态扩展到思源当前编辑面板的左右边缘。
- 功能区高度动态填满标题下方的剩余可视区域，不再固定为最多 720px。
- 面板和窗口尺寸变化时自动重新计算，正文标题仍保留原有边距。

## v1.6.5

- 修复同步回滚到旧插件后重新出现外框、阴影和手动保存按钮的问题。
- 脑图画布改为与思源文档一致的白色背景，消除无边框后仍可见的浅灰矩形边缘。
- 使用新版本参数强制刷新同版本内已缓存的编辑器静态资源。

## v1.6.4

- Excel 保留原始 workbook，只更新实际编辑的单元格；未编辑公式、合并、样式、列宽和隐藏区域继续保留。
- 仅允许编辑 `.xlsx`，不再把 CSV/XLS/XLSM/XLSB 错写为 XLSX 内容。
- SheetJS 升级到官方安全版 0.20.3。
- FreeMind 保存保留未知节点属性、链接、备注和非任务图标。
- 修复 `%` 文件名、附件路径穿越和文档树观察器性能问题。
- 旧笔记中的脑图/表格嵌入会自动刷新到当前插件版本，避免历史 `v=` 参数继续命中旧缓存。

## v1.6.3

- 恢复 Excel 表格内部的浅色网格线，同时保持整个嵌入块无外框、无阴影。
- 修复表格重新打开后可能停在中间行的问题，默认回到左上角。
- 修复脑图操作提示遮挡左侧工具栏的问题。
- 脑图和表格改为响应式高度，小屏幕中可以看到完整工具区和 Sheet 标签。
- 脑图保存状态移到左下角，避免和右下角原生缩放工具重叠。

## v1.6.2

- 修复 NAS/LAN HTTP 环境中 `crypto.subtle.digest` 不可用导致脑图和表格无法载入、保存的问题。
- 修复附件 `modTime` 单位错误造成修改时间落到 1970 年并影响同步的问题。
- 移除手动“保存到思源”按钮，保留 700ms 自动保存、失败恢复和冲突确认。
- 移除云文档外层边框、圆角、主题阴影和右下角拖拽装饰。
- 修复连续编辑时旧保存误删新恢复缓存的竞态。

## v1.6.1

- 附件写回成功后更新文档同步修订标记，使修改进入思源同步链路。
- 同步标记失败不再把已经完成的附件写入误报为失败。

## v1.6.0

- Excel 与 FreeMind 脑图修改直接写回思源 `assets` 附件。
- 增加本机失败恢复、跨页面/设备冲突检测和保存状态提示。
- 为编辑器静态资源加入版本参数，降低升级后旧缓存继续生效的概率。

## 升级提示

安装后请重启思源，或禁用后重新启用插件。若思源同步设置为手动选择方向，请根据数据来源谨慎选择“上传”或“下载”。

## 后续计划

接下来将重点完善 `.mm` 文件功能和 Excel 功能，包括格式兼容、编辑能力、数据保留和大文件使用体验。
# v1.9.3（开发中）

- 增加 `.xlsx` 工作簿规模与能力分析，超过 500 行或 50 列时明确提示轻量编辑边界。
- 保留原始附件，不对超出范围的内容作无损编辑承诺。
- 强化保存状态与失败反馈的后续开发基线。
# v2.0.0

- 轻量 Excel 超出 500 行或 50 列时进入只读安全预览，避免误改未显示内容。
- 表格和脑图支持 Ctrl+S 手动保存，并用颜色区分保存中、已保存和失败/冲突状态。
- 多文件拖放导入显示当前文件与完成数量，单文件失败会写入诊断记录且不影响其他文件。
- 保留原始附件、恢复缓存、冲突检测、写入后校验和导出校验。
- 继续明确不承诺 VBA、数据透视表、Power Query、复杂图表和完整 Excel 公式引擎的无损编辑。
