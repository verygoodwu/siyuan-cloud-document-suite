# 云文档套件开发计划书

> 稳定基线：v1.9.0  
> 当前开发版本：v1.9.2  
> 目标平台：思源笔记 Windows 桌面端  
> 产品定位：轻量、流畅、可靠的思源文档与表格工作区

## 一、产品方向

云文档套件不以完整替代 Microsoft Excel、Word 或专业脑图软件为目标，而是优先解决思源笔记内的常见办公文件创建、查看、轻量编辑、自动保存和导出问题。

近期开发遵循以下原则：

1. 优先优化思源内的操作体验，而不是扩大复杂格式兼容范围。
2. Excel 只支持常见 `.xlsx` 表格，不实现宏、数据透视表、Power Query、复杂图表和完整公式体系。
3. 暂不针对 NAS、Docker 和网页版增加专用能力；现有可用路径不主动破坏。
4. 暂不引入 Web Worker。保留清晰的数据层接口，真实性能数据达到触发条件后再评估。
5. 小版本用于实现和验证增量能力，大版本只整合已经经过实际使用验证的稳定功能。
6. 数据安全优先于功能数量：自动保存、冲突保护、恢复数据和导出回读不能退化。

## 二、Excel 轻量能力边界

### 重点支持

- `.xlsx` 工作簿和多 Sheet。
- 文本、数字、百分比、日期和常见数字格式。
- 常用单元格样式、合并、行高和列宽。
- 区域选择、复制、粘贴、拖动填充。
- 撤销、重做、查找、排序、筛选和冻结。
- `SUM`、`AVERAGE`、`MIN`、`MAX`、`COUNT`、`ROUND`、`ABS`、`IF` 等轻量公式。
- 自动保存到思源附件、恢复缓存、冲突保护和 `.xlsx` 导出。

### 暂不规划

- VBA、宏、ActiveX 和嵌入对象。
- 数据透视表、切片器和 Power Query。
- 完整 Excel 图表往返兼容。
- 外部工作簿连接和企业数据源。
- 数百种 Excel 函数及完整计算引擎。
- `.xls`、`.xlsb`、`.xlsm` 原文件写回。
- 对复杂工作簿作无损编辑承诺。

遇到超出范围的文件时，以保留原件和明确提示为原则，不静默覆盖高级内容。

## 三、版本策略

不增加单独的测试版发布通道。开发中的小版本先在本机和测试文件中验证，通过发布门槛后再作为正式版本上传集市。

### v1.8.2：基础输入与行列尺寸（已完成）

已完成：

- 数字输入保存为数值单元格。
- 百分比输入保存为数值和百分比格式。
- 前导零编号默认保留为文本。
- 支持拖动调整行高、列宽。
- 支持双击自动列宽和恢复默认行高。
- 行列尺寸支持撤销、重做、恢复、自动保存和导出回读。

验收标准：

- 现有功能无回归。
- 保存并重新打开后数值类型和尺寸保持一致。
- 简约模式与编辑模式显示一致。
- Windows 思源桌面端无控制台错误。

### v1.8.3：行列日常操作（已完成）

已完成：

- 在当前单元格或整行、整列选区的位置插入行、列。
- 删除当前行列或选中的整行、整列区域。
- 结构变化同步调整本 Sheet 和跨 Sheet 公式引用。
- 同步调整合并区域、行高、列宽、冻结、筛选和插件图表范围。
- 使用工作簿级结构历史保证跨 Sheet 公式也能可靠撤销、重做。
- 超出 500 行、50 列轻量可视范围时阻止结构操作，避免误改隐藏内容。
- 行号、列标支持 Shift 与拖动连续选择，并提供右键菜单和整行整列快捷键。
- 多行多列删除显示数量与范围；操作期间防重复触发，完成后保持滚动位置和结构选区。
- 历史快照只完整保存目标 Sheet，其他 Sheet 仅记录相关公式，避免复制无关的大型数据区。

### v1.8.4：输入与查找效率（本机开发与回归已完成）

已实现：

- 查找、替换和全部替换，可限定大小写、当前选区和公式原文。
- 批量替换合并为一次公式重算、撤销和恢复操作，并防止默认误改公式结果。
- 严格日期、人民币货币与普通文本之间的轻量类型识别和格式切换。
- 常用公式候选、函数参数提示和错误说明。
- Home、End、Ctrl+Home、Ctrl+End、Ctrl+方向键及 Shift 扩展区域跳转。
- 大范围粘贴的处理状态、写入数量和超限截断提示。

验证完成：30 项自动测试、类型检查、生产构建、发布包结构检查，以及 Windows 思源真实保存、关闭重开和控制台回归。当前尚未提交、打标签、推送或上传集市。

### v1.8.5：编辑手感收口（本机开发与回归已完成）

已实现：

- 直接输入替换当前单元格，F2/双击进入单元格内部编辑。
- Esc 恢复本次编辑前内容，并移除对应的临时恢复操作。
- 选区非空计数、数字数量、求和和平均值。
- 公式候选的上下方向键、Enter/Tab 选择和括号内光标定位。
- 合并单元格感知的方向键导航。
- 500×50 全轻量可视范围的选区统计压力测试。

验证完成：32 项自动测试、类型检查、生产构建、发布包结构检查，以及 Windows 思源真实编辑、取消、保存、关闭重开和控制台回归。当前尚未提交、打标签、推送或上传集市。

### v1.9.0：轻量表格稳定版（本机开发与回归完成）

当前已完成：

- 写入思源后重新读取附件并校验 SHA-256，验证通过后才清除恢复缓存。
- 导出前回读校验 Sheet、公式、合并区域和插件视图状态。
- 保存失败或冲突未解决时保持编辑模式与明确错误状态。
- 导出过程防重复触发，窄窗口统一查找面板、统计栏和公式候选布局。
- 自动测试扩充至 35 项。

验证完成：35 项自动测试、类型检查、生产构建、发布包结构检查，以及 Windows 思源真实保存、写入后校验、刷新重开、导出回读和控制台回归。当前尚未提交、打标签、推送或上传集市。

v1.9.0 不以新增大量功能为目标，而是作为轻量表格体验的稳定里程碑。

### v1.9.1：脑图查找与大纲（本机开发与回归完成）

当前已实现：

- Ctrl+F 节点查找、Enter/Shift+Enter 和 F3/Shift+F3 循环跳转。
- 搜索已折叠节点并按需展开祖先，将结果定位到侧栏外的可视区域。
- 可折叠节点大纲，以及画布选择与大纲高亮的双向联动。
- Tab 新增后自动选中并进入直接输入；尚未输入时 Ctrl+Z 可撤销整个新增节点。
- 新增快捷键帮助面板、Ctrl+/ 分支折叠、Ctrl+Home 回到中心主题。
- 新增安全的分支聚焦、始终可见的“返回完整脑图”入口，并确保保存、导出和历史仍使用完整脑图。
- 优化中文输入法和长文本编辑框尺寸，修复收起分支作为聚焦根时 Tab 新增的边界错误。
- 隐藏思源 iframe 外层地球拖动标识，保留布局工具中文提示。
- 新增 6 项脑图算法与快捷键上下文测试，自动测试总数增加到 41 项。

验证完成：41 项自动测试、类型检查、生产构建、发布包结构检查，以及 Windows 思源真实查找、大纲、快捷键帮助、中文长文本输入、分支折叠、分支聚焦、Tab 直接输入、Ctrl+Z 撤销、自动保存、刷新重开和控制台回归。当前尚未提交、打标签、推送或上传集市。

### v1.9.2：脑图操作一致性与大图流畅度（本机开发与回归完成）

当前已实现：

- 搜索期间只临时展开结果祖先；关闭搜索后恢复进入搜索前的收起状态，并保留用户搜索期间主动修改的分支状态。
- 合并连续脑图刷新、连线重绘和节点装饰；输入框存在时跳过无意义的被动全图处理，大纲选择仅更新受影响行。
- Tab 新增子节点、Enter 新增后方同级、Shift+Enter 新增前方同级、Ctrl+Enter 插入父节点后均立即进入文字输入。
- 上述键盘新增节点都使用统一的临时新增跟踪，未输入时 Ctrl+Z 可撤销整个节点。
- Esc 在分支聚焦状态下返回完整脑图，并恢复进入聚焦前的缩放与画布位置；若搜索面板打开则优先关闭面板。
- 新增搜索展开恢复和超过 1000 节点的索引、搜索、大纲性能测试，自动测试总数增加到 43 项。

验证完成：43 项自动测试、类型检查、生产构建和发布包结构检查通过；Windows 思源真实页面已验证搜索临时展开与恢复、Tab/Enter/Shift+Enter/Ctrl+Enter 新增后直接输入与 Ctrl+Z 撤销、Esc 退出分支聚焦与视口恢复、自动保存和刷新重开。回归后原 26 个节点完整保留、没有测试占位节点，干净页面控制台无错误。当前尚未提交、打标签、推送或上传集市。

## 四、后续模块

Excel 稳定后，再按实际反馈依次评估：

1. `.mm` 脑图键盘操作、搜索和大纲联动。
2. Word 内容转换质量和思源块级编辑衔接。
3. Windows 资源管理器拖入、拖出及批量导出。
4. 统一不同格式的文件名、保存状态、搜索、导出和历史入口。

暂不规划多人实时协作、在线分享、企业权限、NAS 服务和完整多维表格引擎。

## 五、性能策略

当前阶段先优化 DOM 渲染、操作次数和保存节流，不提前引入 Worker。

继续保持：

- 最多显示前 500 行、50 列，未显示内容原样保留。
- 普通输入、选择和工具操作应立即反馈。
- 自动保存合并连续编辑，不并发覆盖同一附件。
- 未发生修改时不重复写入附件。
- 页面关闭或切换模式时完成编辑状态收口。

满足以下任一条件后，再建立 Worker 原型：

- 常见支持范围内的表格打开时主界面连续冻结超过 300～500 ms。
- 保存期间明显无法输入或滚动。
- 批量粘贴、排序或公式刷新成为主要性能瓶颈。
- 产品决定支持明显超过当前轻量边界的数据规模。

即使以后引入 Worker，也只作为性能实现方式，不扩大 Excel 兼容承诺。

## 六、质量与测试

每个小版本至少完成：

- 单元测试和恢复重放测试。
- `.xlsx` 读取、修改、保存、重新读取的往返测试。
- TypeScript 类型检查。
- 生产构建和 `package.zip` 内容检查。
- Windows 思源桌面端真实界面回归。
- 自动保存、失败恢复、附件删除和外部冲突回归。
- 简约模式与编辑模式的视觉、焦点和快捷键检查。

禁止发布的情况：

- 存在已知数据丢失或静默覆盖风险。
- 自动保存、撤销、恢复或导出任一核心路径失败。
- 版本号、缓存版本、插件清单和安装包内容不一致。
- 测试或类型检查未通过。

## 七、集市发布流程

1. 拉取远端并确认没有另一台机器的新提交或新标签。
2. 检查工作区，保留与本版本无关的用户改动。
3. 同步 `package.json`、`plugin.json`、README、版本说明和静态资源缓存版本。
4. 执行依赖安装、测试、类型检查、生产依赖审计和生产构建。
5. 检查图标、预览图、`package.zip` 根目录结构和包内版本。
6. 将候选包安装到本机思源，执行真实编辑、自动保存、重启和导出回归。
7. 提交代码，创建新的版本标签；禁止复用已经发布的标签。
8. 推送主分支和标签，等待 GitHub Release 完成。
9. 下载线上发布包重新检查版本、文件和校验值。
10. 等待并验证思源 Bazaar 索引更新后，才视为发布完成。

## 八、当前进度

v1.8.3 第一批代码和候选构建已经完成：

- 行列插入、删除及整行整列选区操作已实现。
- 公式、跨 Sheet 引用、合并、尺寸、冻结、筛选和图表范围迁移已实现。
- 结构操作已接入整工作簿撤销、重做、恢复、自动保存和导出链路。
- 自动测试增加到 26 项并全部通过，类型检查通过。
- Windows 思源桌面端已完成插入、删除、撤销、重做、自动保存、重新打开回读和控制台错误检查。

下一步完成最终发布包审计和边界检查；当前不会自动提交、打标签或上传集市。
