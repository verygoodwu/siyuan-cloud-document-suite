# 云文档套件开发计划书

> 当前稳定基线：v2.1.4
> 当前开发版本：v2.1.12
> 下一稳定里程碑：v2.2.0
> 目标平台：思源笔记 Windows 桌面端，以及单一 Linux/Docker/NAS 内核的桌面浏览器端
> 产品定位：轻量、流畅、可靠的思源本地文档工作区
> 基线验证：2026-09-06，v2.1.9 正在执行自动测试、类型检查、生产构建与真实思源回归

## 一、产品边界

云文档套件不以替代 Microsoft Office、Notepad++、专业 PDF 阅读器或在线协作套件为目标，而是在思源笔记中提供常用文件的创建、查看、轻量编辑、自动保存和可靠导出。

近期开发遵循以下原则：

1. 优先优化思源桌面端体验，同时保证连接同一 Linux/Docker/NAS 思源内核的桌面浏览器具备可靠的保存、恢复和编辑锁；暂不面向手机布局开发。
2. 保持本地优先，不增加账号、服务器、在线协作或第三方云服务依赖。
3. 不与思阅等外部插件联动，避免接口变化、安装顺序和兼容性维护成本。
4. 文本编辑器只处理普通文本；HTML 仅允许在禁用脚本的沙箱中预览，不承担完整浏览器职责。
5. PDF 先复用 Chromium 原生阅读与打印能力，不引入 PDF.js、OCR、批注和翻译系统；Word 转 PDF 只承诺轻量文档的内容和基础排版。
6. Excel 继续维持 500 行 × 50 列的轻量编辑边界，不扩大复杂格式兼容承诺，也不引入 Excel Worker。
7. 小版本逐步实现和验证，大版本只整合已经通过真实使用回归的稳定能力。
8. 数据安全优先于功能数量：自动保存、冲突保护、恢复缓存、写后校验和原件保留不得退化。

## 二、v2.1.4 当前能力盘点

| 模块 | 当前状态 | 下一步判断 |
| --- | --- | --- |
| Excel `.xlsx` | 轻量编辑、公式、格式、结构操作、自动保存、恢复和导出已较完整 | 以稳定维护为主，暂不扩格式 |
| FreeMind `.mm` | 可编辑、搜索、大纲、聚焦、键盘新增和自动保存 | 以缺陷修复为主 |
| 白板 | v2.1.3 首发，v2.1.4 已补齐快捷创建、吸附、重连和性能优化 | 先完成稳定性收口，不继续堆大功能 |
| Word `.docx` | “新建文档（可导出 Word/PDF）”创建原生思源文档；导入 Word 会转换为思源内容并保留原附件 | 直接使用思源已有的 Word/PDF 导出，不重复实现文档导出引擎 |
| PDF | 已支持拖放导入、浏览器原生 iframe 预览并保留原附件 | 增加轻量文件工具栏和异常反馈 |
| XMind `.xmind` | 导入并转换为层级内容 | 暂不做原格式编辑和导出 |
| 普通文本 | v2.1.6 已支持真实文本附件编辑、新建 TXT/HTML、自动保存和 HTML 沙箱预览 | 下一版完善编辑体验 |
| 文件导入 | 支持资源管理器拖入、多文件顺序处理、文本类型识别和单文件失败诊断 | 继续改善结果反馈 |

当前 `package.zip` 约 968 KB。新增编辑器必须按需加载，不能让未打开文本文件的用户承担明显运行时开销。

## 三、版本路线

### v2.1.5：稳定基线收口

目标：先消化 v2.1.4 白板的大规模改动，建立新功能开发前的可靠基线。

- 完成白板撤销/重做局部刷新优化后的 30 分钟压力复测。
- 回归白板文字输入、连接线重连、快捷创建、吸附、保存重开和 SVG/PNG 导出。
- 回归 Excel、`.mm`、拖放导入和附件删除/外部修改冲突，确认白板没有影响旧功能。
- 整理可复用的编辑器启动参数、保存状态和错误提示约定，为文本编辑器接入做准备。
- 文档入口更名为“新建文档（可导出 Word/PDF）”。
- 新建和导入时写入插件文件类型标签，并在一级“创建文件”下方的插件独有“导出文件”菜单中按标签显示 Word/PDF、`.mm`、`.xlsx`、白板 SVG/PNG 和原始附件下载。
- 兼容没有标签的历史文档，通过编辑器地址和附件扩展名识别类型。

验收标准：

- 现有 63 项测试、类型检查和生产构建全部通过。
- 30 分钟压力测试无脚本错误、无持续内存增长，常用操作不出现可重复的秒级卡顿。
- Windows 思源真实页面完成保存、刷新、重启和冲突保护回归。

### v2.1.6：普通文本编辑器核心（已完成）

目标：让文本附件能够在思源内安全打开、编辑并写回原文件。

- 新增独立 `text-editor.html/js`，通过 iframe 按需加载。
- 首批支持 `.txt/.text`、`.html/.htm`、`.css`、`.js/.mjs/.cjs`、`.ts/.tsx/.jsx`、`.json/.jsonc`、`.xml`、`.yaml/.yml`、`.toml`、`.ini/.conf/.config`、`.log`、`.sql` 及常见代码文件；Markdown 继续沿用思源文档导入逻辑。
- 所有格式都按纯文本处理；HTML 可在禁用脚本的沙箱中预览，JavaScript 不会执行。
- 支持 UTF-8 与 UTF-8 BOM；遇到二进制内容或无法可靠解码的文件时停止编辑并保留原件。
- 复用现有 `siyuan-file-store`：800 ms 自动保存、失败恢复、外部修改冲突、附件删除保护、写入后校验和同步修订标记。
- 文档树一级“创建文件”增加“新建文本文件（.txt）”与“新建 HTML 文件（.html）”。
- 资源管理器拖入支持的文本文件后，创建带编辑器的同名思源子文档。
- 首版提供纯文本输入、行号、查找替换、撤销重做、保存状态、自动换行、基础键盘操作和原文件导出。

轻量边界：

- 不超过 5 MB：允许编辑。
- 超过 5 MB 或检测到非 UTF-8 内容：只保留附件或只读查看，避免卡顿和文件损坏。
- 不引入 Worker；若 2 MB 支持范围内出现持续 300 ms 以上主线程冻结，再用真实性能数据评估。

验收标准：

- 中文、英文、Emoji、CRLF/LF、UTF-8 BOM 保存重开后内容一致。
- HTML/JavaScript 内容不会被执行。
- 自动保存失败、附件删除和外部修改均不会静默覆盖。
- 文本编辑器代码只在打开文本文件时加载。

### v2.1.7：文本编辑体验完善（已完成）

目标：在保持轻量的前提下，达到日常代码和配置文件查看编辑的舒适程度。

- 保持原生编辑器轻量基线，先增加当前行高亮、跳转到行和 2/4 空格缩进。
- Tab 支持插入缩进，多行选择支持整体缩进，Shift+Tab 支持反缩进。
- 增加重新加载原文件入口，冲突或外部修改后可以安全放弃本地副本并恢复编辑。
- 记忆缩进宽度、自动换行和 HTML 分栏预览偏好。
- HTML 使用左右分栏沙箱预览，继续禁止脚本执行。
- 只有真实数据表明原生编辑器无法满足性能或编辑需求时，再评估按需引入 CodeMirror 6。

包体预算：

- 相比 v2.1.4，文本编辑阶段的 `package.zip` 增量目标不超过 450 KB。
- `package.zip` 总体目标控制在 1.5 MB 左右；超过预算必须说明来源并删减低频语言包。

### v2.1.8：简易 PDF 阅读体验（已完成）

目标：改善当前“只有 iframe”的体验，同时保持零重型 PDF 引擎。

- 为 PDF 增加统一的文件标题栏、加载状态、错误提示和附件状态。
- 保持现有 PDF 拖放导入流程：上传原始附件、创建同名思源子文档并嵌入阅读器。
- 提供全屏、重新加载、打开原文件、下载原文件和打印入口；页码、搜索和缩放继续使用 Chromium 原生 PDF 工具。
- 文本、Excel、白板补齐与 PDF 一致的全屏/退出全屏入口，脑图保留原生全屏控件；嵌入 iframe 显式允许全屏。
- 加载失败时不显示空白区域，明确提供“打开原文件”和“下载”操作。
- 记住当前文档的阅读位置只作为可选本机体验数据，不写回或改动 PDF 原件。
- 不实现批注、高亮、OCR、翻译、目录解析、双页阅读和思阅联动。
- 不引入 PDF.js；只有原生查看器在目标 Windows/思源版本上存在不可解决的兼容问题时才重新评估。

#### Word 转 PDF 的轻量实现

- 插件“新建文档（可导出 Word/PDF）”创建原生思源文档，因此 Word/PDF 导出直接使用思源已有能力，来源是用户当前看到并已编辑的思源内容。
- 导入的 `.docx` 仍先通过 Mammoth 转成思源内容；后续修改和 PDF 导出都以当前思源文档为准，原始 `.docx` 仅作为附件保留。
- 为当前思源文档生成独立打印视图和打印样式，通过 Chromium 打印窗口选择“另存为 PDF”。
- 支持思源正文中的标题、段落、列表、常见表格、图片、代码块和基础分页；导出前显示“复杂版式可能变化”的明确提示。
- 原始 `.docx` 和当前思源文档始终保留，PDF 转换失败不会修改或替换任何源内容。
- 不承诺页眉页脚、文本框、SmartArt、修订、批注、嵌入字体、精确分页和复杂浮动图片与 Word 完全一致。
- 第一阶段不提供静默的一键生成文件；使用系统打印窗口可以避免额外 PDF 引擎和中文字体包，也允许用户选择纸张、方向和页边距。

验收标准：

- 正常 PDF、较大 PDF、损坏 PDF、文件被删除和路径含中文/空格时均有明确结果。
- 查看和下载始终使用同一份原始附件，不生成有损副本。
- 插件创建或导入的轻量 Word 内容可从当前思源文档生成 PDF，转换失败时源文档和原附件保持不变。
- PDF 功能不增加常驻脚本和明显包体积。

### v2.1.9：统一体验与发布候选

目标：把新增能力与现有编辑器收成同一套产品体验。

- 统一文本、PDF、Excel、脑图和白板的文件名、保存状态、错误反馈、导出/下载和全屏入口。
- 统一窄窗口、明暗主题、键盘焦点和 iframe 高度行为。
- 检查所有静态资源缓存版本，防止升级后继续加载旧编辑器。
- 增加文本和 PDF 的安装包完整性测试、真实浏览器冒烟测试和思源内回归清单。
- 完成从 v2.1.4 升级、禁用重启、覆盖安装和跨设备同步后的读取验证。
- 只修复影响 v2.2.0 发布的缺陷，不再临时加入新功能。

### v2.2.0：轻量文档工作区稳定版

只有 v2.1.5～v2.1.9 的功能全部通过发布门槛后，才发布 v2.2.0。

稳定版包含：

- 经过稳定性收口的 Excel、脑图和白板。
- 可自动保存的普通文本编辑器。
- 独立、轻量、无外部插件依赖的 PDF 导入阅读体验，以及普通 Word 文档的 PDF 打印导出。
- 统一的文件状态、错误处理和安装包验证。

v2.2.0 不增加功能，只允许修复候选版本回归发现的问题。

## 四、实现落点

预计新增或调整以下模块：

- `src/file-types.ts`：增加受支持文本扩展名、二进制和大小边界判断。
- `src/preview-builders.ts`：增加文本编辑器 iframe 和 PDF 阅读外壳。
- `src/index.ts`、`src/drag-drop-controller.ts`：接入文本导入、创建和诊断流程。
- `src/embed-manager.ts`：让文本/PDF 编辑器使用统一尺寸和缓存刷新逻辑。
- `static/text-editor.html/js`：普通文本编辑器界面与行为。
- `static/siyuan-file-store.js`：优先复用，不为文本另建一套保存协议。
- `tests/text-editor.test.mjs`：编码、大小边界、安全、恢复和往返测试。
- `tests/pdf-preview.test.mjs`：路径、异常状态、原附件操作和 Word 打印视图测试。
- `scripts/package.mjs`、`tests/verify-installed.mjs`：检查新增静态文件及包内版本。

## 五、明确延期的功能

- PDF 批注、摘录回链、OCR、翻译和专业阅读模式。
- Word 到 PDF 的复杂版式无损转换，以及无需打印窗口的后台批量转换。
- 与思阅或其他插件建立运行时依赖。
- HTML 实时执行、网页预览或本地 Web 服务器。
- 超大日志文件、十几 MB 代码文件和十六进制编辑。
- 多光标、插件系统、LSP、代码补全、终端和完整 IDE 能力。
- 复杂 Office 格式往返、VBA、数据透视表和完整公式引擎。
- NAS、Docker、手机和网页版专项适配。
- 多人协作、在线分享、权限与云端版本历史。

## 六、质量门槛

每个小版本至少完成：

- 新功能单元测试、失败恢复和往返测试。
- 现有 63 项自动回归不得减少；新增测试只增不减。
- TypeScript 类型检查、生产构建和 `package.zip` 结构检查。
- Windows 思源真实界面下的创建、编辑、自动保存、刷新重开和导出/下载验证。
- 附件删除、外部修改、保存失败、恢复缓存和多页面冲突回归。
- 明暗主题、窄窗口、中文输入法、快捷键和控制台错误检查。

出现以下任一情况禁止发布：

- 已知数据丢失、乱码或静默覆盖风险。
- 自动保存、恢复、冲突保护或原附件下载失败。
- HTML/脚本存在被意外执行的路径。
- 新编辑器导致未使用该功能时仍明显增加内存或启动耗时。
- 版本号、缓存参数、README、发布说明和安装包内容不一致。
- 自动测试、类型检查、构建或真实思源回归未通过。

## 七、集市发布流程

1. 拉取远端并核对另一台机器的新提交和标签。
2. 检查工作区，确认没有无关改动和未处理冲突。
3. 同步 `package.json`、`plugin.json`、README、版本说明和静态资源缓存版本。
4. 执行依赖锁定检查、自动测试、类型检查、生产依赖审计和生产构建。
5. 检查图标、预览图、`package.zip` 根目录结构、包内版本和 SHA-256。
6. 将候选包安装到本机思源，完成所有格式的冒烟、自动保存、重启和导出/下载回归。
7. 提交代码并创建全新版本标签；禁止移动或复用已发布标签。
8. 推送主分支和标签，等待 GitHub Release 构建完成。
9. 下载线上 `package.zip`，重新核对版本、文件、体积和校验值。
10. 等待并验证思源 Bazaar 索引已显示新版本，才视为发布完成。

## 八、执行顺序

当前 **v2.1.12 同步安全与 NAS 编辑保护** 已完成开发和真实思源回归，范围包括单内核编辑锁、404 恢复、两阶段创建、附件逐字节校验、浏览器安全副本及脑图子模块缓存修复；本版本按正式发布流程进入 GitHub Release 与思源集市。
