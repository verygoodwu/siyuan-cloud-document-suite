# 云文档套件

[English](./README.md) | **简体中文**

[![最新版本](https://img.shields.io/github/v/release/verygoodwu/siyuan-cloud-document-suite?label=release)](https://github.com/verygoodwu/siyuan-cloud-document-suite/releases/latest)
[![许可证](https://img.shields.io/github/license/verygoodwu/siyuan-cloud-document-suite)](./LICENSE)

在思源笔记中导入、预览、创建、编辑和导出文档、真实纯文本文件、表格、脑图与白板。支持 Windows 桌面端，以及 Linux/Docker 内核的桌面浏览器端。文本、Excel、FreeMind 脑图与白板的修改会自动写回思源附件，并更新文档同步修订标记；本机缓存仅用于失败恢复。

![功能预览](preview.png)

## v2.1.13 重点更新

- 修复脑图不同层级与左右分支节点双击编辑时输入框位置不一致的问题。
- 所有编辑入口统一按节点实际渲染位置定位，并在输入尺寸变化时保持节点、输入框和连线对齐。

## v2.1.12 重点更新

- 新增真实 TXT、HTML 和常见代码/配置文件编辑，保存后仍是原始附件格式。
- 新增 Chromium 轻量 PDF 阅读器，以及插件文档的思源式直接 PDF 导出。
- 新增按文件类型显示的导出菜单，覆盖文档、表格、脑图、白板、PDF 和 XMind。
- 统一各编辑器的快捷键、视图恢复、保存反馈、恢复副本和窄窗口布局。
- 新增 NAS 单内核编辑锁、两阶段创建、逐字节校验与明确的 404 恢复，并移除同步诊断文件。

本次正式版本包含 v2.1.5–v2.1.12 开发阶段的全部能力，详细记录见[完整更新历史](./RELEASE_NOTES.md)。

## 更新记录（简约版）

- **v2.1.13**：脑图鼠标双击、键盘新增后编辑和直接键入统一使用实际渲染坐标，修复分支层级布局位移和缩放导致的输入框偏移，并在输入尺寸变化时同步更新连线。
- **v2.1.12**：修复脑图编辑器子模块的打包版本替换，防止桌面端和 NAS 浏览器在插件更新后继续复用旧公共模块；包含 v2.1.11 的全部功能。
- **v2.1.11**：新增同一 NAS/Linux/Docker 思源内核内的附件编辑租约、附件上传逐字节校验和文档两阶段创建；成功创建/读取/保存后保留浏览器安全副本，附件被同步隔离并返回 404 时可一键恢复；诊断信息不再写入会参与同步的 `drop-debug.json`。本次同时正式发布 v2.1.5–v2.1.10 开发完成的文本编辑器、PDF 链路、按类型导出和统一编辑体验。
- **v2.1.10（本地开发）**：Word/PDF 导出不再自动调用 Windows 系统打印；桌面端改为思源式 PDF 专用预览与导出链路，支持页面大小、边距、缩放、横向、资源嵌入、标题、子文档、水印和分页设置，并由 Electron `printToPDF` 直接生成 PDF。
- **v2.1.9（本地开发）**：统一文本、Excel、脑图、白板和 PDF 的快捷键、操作反馈与保存失败恢复；记忆光标、滚动、Sheet、选区、脑图视图和 PDF 请求页码；白板新增节点查找，批量或结构操作可从反馈条一键撤销，并改善文件名与窄窗口工具栏。
- **v2.1.8（本地开发）**：新增轻量 PDF 阅读外壳，使用 Chromium 原生阅读器并提供重新加载、原文件打开/下载、打印、全屏、文件大小和损坏/删除提示；文本编辑器新增匹配序号、替换数量确认、行/字符/选区统计、保存时间、恢复副本、保存重试、附件路径及按真实扩展名导出；统一窄窗口横向收纳、状态提示、全屏入口和编辑器切换保存收口，菜单提升失败时保留标准“插件”子菜单入口。
- **v2.1.7（本地开发）**：增强轻量文本编辑体验，新增当前行高亮、跳转到行、2/4 空格缩进、Shift+Tab 反缩进、重新加载原文件、偏好记忆和 HTML 左右分栏预览；继续使用原生编辑器，不增加重型依赖。
- **v2.1.6（本地开发）**：新增真实纯文本附件编辑器，支持新建 TXT/HTML、导入常见文本格式、自动保存、恢复和冲突保护；保持 UTF-8 BOM 与换行符，并提供不会执行脚本的 HTML 沙箱预览。
- **v2.1.5（本地开发）**：文档树入口更名为“新建文档（可导出 Word/PDF）”；在一级“创建文件”下方新增插件独有的“导出文件”菜单，按插件文件类型标签显示 Word/PDF、`.mm`、`.xlsx`、白板 SVG/PNG 或原始 PDF/XMind 下载，并兼容识别没有标签的旧文档。
- **v2.1.4**：完成两批白板交互优化：点击/拖动式四向快捷创建、5 像素防误拖、工具栏动态避让、自然平移缩放、对齐与等距吸附、连接目标高亮与端点重连、连线文字、Alt 拖动复制及缩放到选择；旧白板文件保持兼容。
- **v2.1.3**：首次发布可编辑白板，并包含 v2.1.0–v2.1.2 开发阶段的全部能力；重构文字编辑层，修复空节点无法稳定输入、编辑框文字未居中及双击重复初始化，新增真实浏览器文字输入与回写测试。白板独立接入，不改变既有表格、脑图、Word 与导入流程。
- **v2.1.2（本地开发）**：完善白板输入逻辑，新建图形立即输入，支持双击、Enter/F2 和选中后直接打字；修复创建编辑时序和含文字白板的 PNG 导出；建立覆盖全部白板主功能的完整测试矩阵。尚未推送或上传集市。
- **v2.1.1（本地开发）**：修复白板编辑文字未垂直居中，以及连续点击或双击可能误判为空白画布并重复创建节点的问题；补充真实浏览器回归测试。
- **v2.1.0（本地开发）**：新增飞书风格可编辑白板，支持无限画布、文本、便签、常用图形、连接线、自由画笔、图片、框选、多对象对齐与等距分布、分组、分区、图层顺序、内置模板、自动排版、撤销重做、自动保存、恢复、冲突保护以及 SVG/PNG 导出。

- **v1.9.3（开发中）**：增加工作簿规模与能力边界提示，超出轻量范围时避免静默编辑；继续强化保存状态、恢复和失败提示。
- **v1.9.4（开发中）**：增加 Ctrl+S 手动保存入口，并为保存状态提供可区分的视觉反馈。
- **v1.9.5（开发中）**：统一多文件导入进度和单文件失败诊断，导入过程中显示当前文件与完成数量。
- **v2.0.0**：完成轻量体验稳定版收口；复杂工作簿明确能力边界，表格和脑图统一保存反馈，导入过程显示进度并保留单文件失败诊断。

- **v1.9.2**：搜索结束后恢复各分支原有收起状态；合并脑图刷新与装饰任务，改善大图操作流畅度；统一 Tab、Enter、Shift+Enter、Ctrl+Enter 的新增后直接输入和 Ctrl+Z 撤销；Esc 可退出分支聚焦并恢复进入前视口。
- **v1.9.1**：新增脑图节点查找、结果循环跳转、可折叠大纲和画布选中联动；Tab 新增后自动选中并直接输入，Ctrl+Z 可撤销键盘新增节点；补充快捷键面板、Ctrl+/ 分支折叠、Ctrl+Home 回根、分支聚焦与常驻返回入口，并改善中文长文本输入框和聚焦状态历史安全。
- **v1.9.0**：收口轻量表格稳定性，新增写入后 SHA-256 校验、导出 Sheet/公式/合并区/视图状态回读校验、保存失败阻止静默退出、重复导出保护和窄窗口布局整理。
- **v1.8.5**：统一表格编辑手感，直接输入替换当前单元格，F2/双击进入单元格内编辑，Esc 取消本次输入；新增选区计数、求和、平均值、全键盘公式候选和合并单元格感知导航。500×50 全范围统计仍保持轻量，无需 Worker。
- **v1.8.4**：新增查找替换与全部替换，可限定大小写、当前选区或公式原文；支持严格日期和人民币输入，补充公式提示与错误说明，完善 Home/End、Ctrl+方向键跳转和大范围粘贴反馈，仍保持无 Worker 的轻量实现。
- **v1.8.3**：新增选中位置插入/删除行列，以及行列 Shift/拖动连续选择、右键入口和快捷键；公式、跨 Sheet 引用、合并区域、行列尺寸、冻结、筛选与图表范围会同步迁移，并支持工作簿级撤销重做、恢复和保存回读。
- **v1.8.2**：优化轻量表格输入，数字和百分比按真实数值保存，前导零编号保持文本；新增行高、列宽拖动与双击自动调整，并支持撤销、恢复和导出回读。
- **v1.8.1**：完善脑图节点对齐、收缩控件、编辑与画布位置稳定性；扩展表格编辑和格式工具；将创建文件提升到文档树一级菜单；修复编辑器依赖缓存版本，并把透明集市图标压缩到限制以内。
- **v1.8.0**：增强 Excel 区域选择、复制粘贴、撤销重做、键盘导航与当前 Sheet 查找；新增轻量公式计算和依赖刷新，并优化集市图标黑边。
- **v1.7.0**：完善 `.mm` 默认层级样式、飞书式直线连接、空节点与格式显示，移除概要/自定义连接功能；修复新建脑图和 Excel 复用旧附件的风险，并增强删除、同步冲突和恢复保护测试。
- **v1.6.8**：脑图“完成”改为直接切换灰色删除线状态，新增可保存的层级样式，并修正任务圆点及下划线显示。
- **v1.6.7**：修复进入表格或脑图时闪烁后变大的问题。
- **v1.6.6**：表格和脑图功能区自适应贴合编辑面板。
- **v1.6.5**：移除脑图、表格的外框、阴影和残留视觉边缘。
- **v1.6.4**：增强 Excel 数据保留、`.mm` 字段保留、安全性与缓存刷新。
- **v1.6.3**：恢复 Excel 网格线并优化编辑器响应式布局。
- **v1.6.2**：修复 `digest`、附件时间戳和连续自动保存问题。
- **v1.6.1**：增加文档同步修订标记。
- **v1.6.0**：Excel 与 `.mm` 开始自动写回思源附件。
- **v1.5.0**：增强脑图方向、格式、任务状态和静态资源防缓存。
- **v1.4.2**：修复 Excel 新增可见行列无反馈的问题。
- **v1.4.1**：增加 Linux/Docker 与 `browser-desktop` 支持。
- **v1.4.0**：上线办公文档、表格、PDF、XMind 和 FreeMind 导入预览能力。

## 后续计划

接下来优先完善思源内的轻量编辑体验；白板已补齐第二阶段的框选、多对象排版、分组、分区、图层和模板能力，暂不扩大复杂 Excel 兼容范围。

## 功能

- 将 Windows 资源管理器中的文件拖到文档树中的文档或笔记本，自动上传附件并创建同名子文档。
- PDF 使用轻量阅读外壳调用 Chromium 原生阅读器，支持重新加载、原文件打开/下载、打印、全屏和损坏/删除反馈，不引入 PDF.js。
- TXT、HTML、CSS、JavaScript、JSON、XML、YAML、日志和常见代码/配置文件可作为真实附件编辑，支持自动保存、恢复副本、冲突保护、匹配序号、替换确认、状态统计、换行符保持和按原扩展名导出；HTML 可在禁用脚本的沙箱中分栏预览。
- Excel 工作簿可编辑，支持多 Sheet、新增、删除、重命名、区域复制粘贴、行列插入删除与尺寸调整、撤销重做、键盘导航、查找替换、常用公式计算、自动写回思源和导出 `.xlsx`。
- Word `.docx` 转换为可阅读的思源文档内容，同时保留原始附件；“新建文档（可导出 Word/PDF）”使用思源块编辑，可直接导出 `.docx`，桌面端通过思源式专用预览生成 PDF，浏览器端保留手动打印后备。
- XMind `.xmind` 支持新版 `content.json` 与旧版 `content.xml`。
- FreeMind/Freeplane `.mm` 可实时编辑、拖动节点、自动写回思源并导出标准 `.mm`。
- 白板支持文本、便签、矩形、椭圆、菱形、图片、自由画笔与自动跟随节点的连接线，并提供按需四向快捷创建、拖动创建预览、连接线文字与端点重连、框选、吸附、对齐与分布、Alt 拖动复制、分组、分区、图层顺序、模板、自动排版、缩放到选择、撤销重做、自动写回思源以及 SVG/PNG 导出。
- 文档与笔记本右键菜单支持新建白板、脑图、Word 文档和 Excel 工作簿。
- 同名文件自动追加 `(2)`、`(3)`，多文件按顺序导入，单个文件失败不会中断其余任务。

## 支持范围

| 类型 | 导入 | 预览/编辑 | 导出 |
| --- | --- | --- | --- |
| 普通文本 `.txt/.html/.css/.js/.json…` | ✓/新建 | 真实附件编辑；HTML 安全预览 | 原文件 |
| PDF | ✓ | Chromium 原生阅读、全屏、打印与异常反馈 | 原文件 |
| Excel `.xlsx` | ✓ | 多 Sheet 编辑 | `.xlsx` |
| Word `.docx` | ✓ | 内容预览/思源编辑 | `.docx` |
| XMind `.xmind` | ✓ | 层级内容 | 原文件 |
| FreeMind `.mm` | ✓ | 实时脑图编辑 | `.mm` |
| 云文档白板 | 新建 | 实时矢量编辑 | SVG / PNG |

旧版 `.doc` 和网页链接预览暂不支持。

NAS Docker 用户请使用桌面浏览器访问思源。文件会从当前电脑上传到 NAS 上的思源工作空间；手机浏览器端暂不支持。

## 安装与开发

从 GitHub Releases 下载 `package.zip`，解压到 `<思源工作空间>/data/plugins/siyuan-cloud-document-suite/`，重启思源后在“设置 → 集市 → 已下载”中启用。

```powershell
pnpm install
pnpm build
```

`pnpm build` 会生成 `dist/` 和可直接发布到 GitHub Release 的 `package.zip`。

## 隐私

文档解析和编辑在本地完成。插件不会主动上传文件到第三方服务；附件仅保存到当前思源工作空间。

## 功能建议与问题反馈

如果你希望插件增加新的功能，或在使用过程中发现问题，欢迎发送邮件至 [wujiaqi8868@gamil.com](mailto:wujiaqi8868@gamil.com)。我会查看反馈，并根据功能的适用范围、实现难度和插件整体规划，评估是否将其加入云文档套件。

云文档套件仍在持续开发中。我正在陆续补充更多功能并修复已发现的问题。欢迎各位用户和开发者试用、反馈，也感谢大家对这个插件的改进建议。

## 许可证

[MIT](LICENSE)
